{
  "name": "site-generator",
  "version": "0.0.1",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^14.6.3",
    "typescript": "^3.9.5"
  },
  "scripts": {
    "start": "node bin/main.js",
    "clean": "rm -rf ./bin",
    "build": "tsc",
    "run": "rm -rf ./bin && tsc && node bin/main.js"
  },
  "dependencies": {
    "@types/markdown-it": "^10.0.2",
    "commander": "^6.1.0",
    "escape-string-regexp": "^4.0.0",
    "markdown-it": "^11.0.0"
  }
}
